## 时钟同步与异步
同步与异步最大的差异的差异是在会不会需要时钟信号
因为同步传输需要使用时钟信号，因此在严格遵守时钟的情况下，信息传输的传输量很大，传输速度很快。
而异步传输虽然不用时钟，但是在信息前后都需要加上额外的字节信息，导致传输速率和量会相对较小。

## 串口
首先，现在cpu都采用并行数据字符处理与输出，而下位机一般采用并行的方式进行收发消息。
因此，我们需要串口（串行通信接口/COM接口）来进行接受并行，传递出串行。或者接受串行，传递出并行。

